import 'package:flutter/material.dart';

class AppColors {
  // Fondo principal
  static const Color background = Color(0xFF181A20);
  // Tarjetas y campos
  static const Color card = Color(0xFFFFFFFF);
  // Azul principal
  static const Color primary = Color(0xFF23406E);
  // Amarillo/acento
  static const Color accent = Color(0xFFFFD600);
  // Texto principal
  static const Color textPrimary = Color(0xFFFFFFFF);
  // Texto secundario
  static const Color textSecondary = Color(0xFFB0B3B8);
  // Sombra
  static const Color shadow = Color(0x33000000);
  // Error
  static const Color error = Color(0xFFFF4C4C);
  // Color de borde
  static const Color border = Color(0xFFE0E0E0);

  // Colores secundarios
  static const Color secondary = Color(0xFF03DAC6);
  static const Color secondaryLight = Color(0xFF66FFF8);
  static const Color secondaryDark = Color(0xFF00A896);

  // Colores terciarios
  static const Color tertiary = Color(0xFFFF4081);
  static const Color tertiaryLight = Color(0xFFFF80AB);
  static const Color tertiaryDark = Color(0xFFF50057);

  // Colores de fondo
  static const Color surface = Color(0xFFFFFFFF);
  static const Color textHint = Color(0xFF9E9E9E);

  // Colores de estado
  static const Color success = Color(0xFF4CAF50);
  static const Color warning = Color(0xFFFFC107);
  static const Color info = Color(0xFF2196F3);

  // Colores de modo oscuro
  static const Color darkBackground = Color(0xFF121212);
  static const Color darkSurface = Color(0xFF1E1E1E);
  static const Color darkError = Color(0xFFCF6679);
  static const Color darkTextPrimary = Color(0xFFFFFFFF);
  static const Color darkTextSecondary = Color(0xFFB0B0B0);
  static const Color darkTextHint = Color(0xFF757575);
} 